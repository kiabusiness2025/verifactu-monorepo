{
  "buildCommand": "pnpm install --no-frozen-lockfile && pnpm turbo build --filter=verifactu-landing",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": "nextjs",
  "outputDirectory": "apps/landing/.next"
}
