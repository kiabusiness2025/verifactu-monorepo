{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../.. && corepack enable && corepack prepare pnpm@10.27.0 --activate && pnpm turbo run build --filter=verifactu-landing",
  "installCommand": "cd ../.. && corepack enable && corepack prepare pnpm@10.27.0 --activate && pnpm install --frozen-lockfile",
  "framework": "nextjs"
}
