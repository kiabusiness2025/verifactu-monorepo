{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "corepack enable && pnpm install --frozen-lockfile && pnpm --filter=verifactu-app build",
  "outputDirectory": "apps/app/.next",
  "framework": "nextjs"
}
