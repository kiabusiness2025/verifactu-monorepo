import '@testing-library/jest-dom'

describe('Verifactu App', () => {
  it('passes basic test', () => {
    expect(true).toBe(true)
  })

  it('can perform basic math operations', () => {
    expect(1 + 1).toBe(2)
  })
})
