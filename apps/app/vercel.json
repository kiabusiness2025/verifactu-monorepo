{
  "framework": "nextjs",
  "installCommand": "cd ../.. && npm install --prefer-offline --no-audit && npm --prefix apps/app run build",
  "buildCommand": "echo 'Build already completed during install'"
}
