[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/holded-mcp/Dockerfile"
buildContext = "apps/holded-mcp"
buildCache = false

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
